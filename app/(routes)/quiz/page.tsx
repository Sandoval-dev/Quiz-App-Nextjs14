import React from 'react'
import {
    ClerkProvider,
    SignInButton,
    SignedIn,
    SignedOut,
    UserButton
  } from '@clerk/nextjs'

const page = () => {
  return (
    <div className='text-white'>
      quiz
    </div>

  )
}

export default page
